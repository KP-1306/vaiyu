import { useState } from 'react';import { api } from '../lib/api';export default function Precheck(){const [name,setName]=useState('');const [smoking,setSmoking]=useState(false);const [eta,setEta]=useState('14:00');const [msg,setMsg]=useState('');async function submit(){await api.precheck({ booking:'DEMO', name, prefs:{smoking}, eta });setMsg('Pre-check-in received. We will prepare your room.')}return (<main className='max-w-xl mx-auto p-6 space-y-3'><h1 className='text-2xl font-bold'>Pre-check-in</h1><input value={name} onChange={e=>setName(e.target.value)} placeholder='Full name' className='w-full p-2 border rounded'/><label className='flex items-center gap-2'><input type='checkbox' checked={smoking} onChange={e=>setSmoking(e.target.checked)}/>Smoking room preferred</label><label className='block'>ETA: <input value={eta} onChange={e=>setEta(e.target.value)} className='p-2 border rounded'/></label><button onClick={submit} className='px-4 py-2 bg-sky-600 text-white rounded'>Submit</button>{msg && <p className='text-green-700'>{msg}</p>}</main>)}