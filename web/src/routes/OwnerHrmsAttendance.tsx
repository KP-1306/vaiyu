// web/src/routes/OwnerHrmsAttendance.tsx
// Wrapper that renders the full OwnerHRMS module.
// Useful if you later want a dedicated route that still uses the same HRMS logic.

import OwnerHRMS from "./OwnerHRMS";

export default function OwnerHrmsAttendance() {
  return <OwnerHRMS />;
}
