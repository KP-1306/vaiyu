import { useEffect, useState } from 'react';import { useParams, Link } from 'react-router-dom';import { api } from '../lib/api';export default function Hotel(){const {slug}=useParams();const [data,setData]=useState<any>(null);useEffect(()=>{api.hotel(slug||'').then(setData).catch(console.error)},[slug]);return (<main className='max-w-2xl mx-auto p-6 space-y-4'><h1 className='text-2xl font-bold'>{data?.name||'Property'}</h1><p className='text-gray-600'>{data?.description||'Microsite placeholder'}</p><div className='grid grid-cols-2 gap-3'><Link className='px-4 py-3 bg-white rounded shadow' to='/stay/DEMO/menu'>Open Guest Menu</Link><Link className='px-4 py-3 bg-white rounded shadow' to='/precheck/DEMO'>Pre-check-in</Link></div></main>)}