import { useEffect, useState } from 'react';import { api } from '../lib/api';export default function Bill(){const [folio,setFolio]=useState<any>(null);useEffect(()=>{api.folio('DEMO').then(setFolio).catch(console.error)},[]);return (<main className='max-w-xl mx-auto p-6 space-y-4'><h1 className='text-2xl font-bold'>Your Bill</h1><ul className='bg-white rounded shadow divide-y'>{(folio?.lines||[]).map((l:any,i:number)=>(<li key={i} className='p-3 flex justify-between'><span>{l.description}</span><span>₹{l.amount}</span></li>))}</ul><div className='text-right font-semibold'>Total: ₹{folio?.total||0}</div></main>)}